spring:
  datasource:
    url: jdbc:mysql://localhost:3308/lowcode_platform?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root
    password: hhdtest
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

platform:
  security:
    jwt-secret: mySecretKey123456789012345678901234567890
    token-expire: 86400000

server:
  port: 8080

logging:
  level:
    com.platform: DEBUG
